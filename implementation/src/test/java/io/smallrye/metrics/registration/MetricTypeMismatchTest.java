package io.smallrye.metrics.registration;

public class MetricTypeMismatchTest {

    // TODO, registering a metric using metricregistry.TYPE(metadata,....) when metadata.type != TYPE, should throw an exception

}
